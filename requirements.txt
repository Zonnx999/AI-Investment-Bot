# === Phase 1: 데이터 수집 ===
yfinance>=0.2.40           # 주가, 재무제표 (무료, 야후파이낸스)
fredapi>=0.5.2             # 미국 거시지표 (무료, FRED)
pycoingecko>=3.1.0         # 비트코인/이더리움 (무료)
requests>=2.31.0           # HTTP 호출

# === 데이터 분석 ===
pandas>=2.2.0              # 표 형태 데이터
numpy>=1.26.0              # 수치 계산
scipy>=1.13.0              # 통계 (VaR 계산에 사용)

# === 시각화 ===
matplotlib>=3.8.0
plotly>=5.20.0

# === LLM (Phase 4) ===
anthropic>=0.40.0          # Claude API
python-dotenv>=1.0.1       # .env 파일 로딩

# === 챗 UI (Phase 6) ===
streamlit>=1.36.0          # 가장 빠르게 챗 UI 만드는 라이브러리

# === 개발 편의 ===
jupyter>=1.0.0             # 노트북
ipykernel>=6.29.0
